pipeline {
  agent any 
  stages {
    stage ('Init') {
      steps { 
        echo 'Hi this is demo pipe'
      }
    }
    stage ('Build') {
      steps {
        echo 'building stage'
      }
    }
  }
}
